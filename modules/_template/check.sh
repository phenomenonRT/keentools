#!/bin/sh
# check.sh — необязательный скрипт. Если он есть, менеджер использует
# ИМЕННО ЕГО, чтобы решить, установлен ли проект — вместо файла-метки
# в state/. Так статус "установлен" отражает реальное состояние системы
# (пакет, бинарь, init.d-скрипт), а не то, что когда-то отработал
# install.sh менеджера.
#
# Код возврата: 0 = проект установлен, не-0 = не установлен.
# Вывод в stdout/stderr не используется менеджером — можно оставить пустым.

# TODO: замените на реальную проверку, например:
#   opkg list-installed | grep -q "^myproject "
#   [ -x /opt/sbin/myproject ]
#   /opt/etc/init.d/S99myproject status >/dev/null 2>&1

exit 1
