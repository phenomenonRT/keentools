#!/bin/sh
# worker.sh — тело фонового процесса демо-сервиса.
# Не вызывается менеджером напрямую — только через start.sh.
# data_dir="$1"
# log_file="$data_dir/example-service.log"
# while true; do
#     echo "$(date '+%Y-%m-%d %H:%M:%S') — сервис работает" >> "$log_file"
#     sleep 5
# done
